{
  "buildCommand": "flutter build web --release --dart-define=BASE_URL=https://jethrona.pythonanywhere.com/api",
  "outputDirectory": "build/web",
  "devCommand": "flutter run -d chrome",
  "rewrites": [{ "source": "/:path*", "destination": "/index.html" }]
}
